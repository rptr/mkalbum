# HTML photo album generator

This simple script takes a folder of images and outputs a HTML file with thumbnail links to the images.

**TODO**
  * multi-page (multiple html files, links to next/previous page)
  * parameters (thumbnail size, photos per page)